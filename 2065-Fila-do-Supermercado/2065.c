#include <stdio.h>

int tempo(N, M, v, c){
    int cont, horas;
    for (cont = 0; cont < M; i++)
    {

    }
}

int main() {
    int N, M, i, j;
    scanf("%d %d", &N, &M);
    int v[N], c[M];

    for(i = 0; i < N; i++){
        scanf("%d", &v[i]);
    }

    for(j = 0; j < M; j++){
        scanf("%d", &c[j]);
    }


}